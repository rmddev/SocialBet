	<nav class="navbar navbar-default">
		
		<div class="container">
			
			<%= link_to "SocialBet", root_path, class: "navbar-brand" %>

			<div class="pull-right">

				<% if user_signed_in? %>
				
					<%= link_to "Sign Out", destroy_user_session_path, :method => :delete, class: "btn btn-default navbar-btn" %>

					<%= link_to "Add Bet", new_bet_path, class: "btn btn-primary navbar-btn"  %>

				<% else %>

					<%= link_to "Sign In", new_user_session_path, class: "btn btn-default navbar-btn" %>

					<%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary navbar-btn"  %>

				<% end %>

			</div><!-- /.pull-right -->

		</div><!-- /.container -->

	</nav><!-- /.navbar navbar-default -->